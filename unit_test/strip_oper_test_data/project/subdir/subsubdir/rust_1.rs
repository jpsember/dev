hello
                prif!(
                    vb,
                    "...creating target schema and table:",
                    &target_schema_name,
                    &self.targettable
                );
                Table::create(zzzzz);

hello
                pr!(vb,"...creating target schema and table:",
                  &target_schema_name,
                                                        &self.targettable);

                Table::create(zzzz);

