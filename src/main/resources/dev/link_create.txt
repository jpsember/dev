
##################################################
# Install the driver script to the bin directory
##################################################

if [ "$DRIVER" -ne "0" ]; then
  if [ ! -f $LINK ]; then
    cp .jsproject/driver.sh $LINK
  fi
fi
