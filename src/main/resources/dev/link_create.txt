
##################################################
# Install the driver script to the bin directory
##################################################

if [ "$DRIVER" -ne "0" ]; then
  if [ ! -f $LINK ]; then
    DIR=$(pwd)
    cp $DIR/.driver.sh $LINK
  fi
fi
