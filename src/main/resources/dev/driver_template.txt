#!/usr/bin/env bash
set -e

APP=[!app_name]
MVN=$HOME/.m2/repository
DIR=$(pwd)

###### Custom statements start ##### {~custom1:
###### Custom statements end   ##### ~}

if [ -f "target/classes/[!package_name_slashes]/[!main_class_name].class" ]; then
  APPLOC="target/classes"
else
  APPLOC="$MVN/com/jsbase/$APP/1.0/$APP-1.0.jar"
fi

java -Dfile.encoding=UTF-8 -classpath $APPLOC:$MVN/commons-io/commons-io/2.6/commons-io-2.6.jar:$MVN/com/jsbase/base/1.0/base-1.0.jar [!package_name]/[!main_class_name] "$@"
