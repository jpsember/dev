#!/usr/bin/env sh
set -eu

FILE="mk"
if [ -f ${FILE} ]; then
	$FILE "$@"
	exit 0
fi

FILE=.jsproject/make.sh
if [ ! -f ${FILE} ]; then
	echo "Calling dev to create a make file"
	dev createmake
fi

${FILE} "$@"
