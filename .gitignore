# OSX files
#
.DS_Store

# Files marked for later removal, or temporary omission
#
_OLD_*
_SKIP_*

# We can't use the dev program itself to generate the driver and makefile, so
# we need to track the .jsproject directory (in case it was omitted higher up):
#
!.jsproject

# Sublime text editor
#
*.sublime-project
*.sublime-workspace

# PyCharm (and other JetBrains) IDE
#
.idea

# Eclipse IDE
#
.classpath
.project
.settings
eclipse_preferences.epf

# Compile output
#
target

src/main/java/dev/gen

# For testing
#
sample_project*
