enum dev.gen.OsType;

fields {
  string tag;
  string long_name;
  OsType os_type;
  string url;
  int port;
  string user;

  // Directory containing the project; the one containing the git repo (on the remote machine)
  //
  File project_dir;
}
